# Standalone data reference — matches the data: block in config.yaml.
kg_split_overlap: 0.0
seed: 42
max_triples_per_entity: 100
max_triples_per_kg: 500
max_entity_mentions: 5
wikidata_cache_dir: "~/.cache/debate_kg/wikidata"
