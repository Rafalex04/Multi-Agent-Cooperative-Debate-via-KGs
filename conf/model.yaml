# Standalone model reference — matches the model: block in config.yaml.
# Override individual keys: python -m debate_kg.main model.expert_temperature=0.5
expert_model: "llama3.1:8b"
expert_temperature: 0.7
judge_temperature: 0.0
